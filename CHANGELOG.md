# Changelog

כל השינויים הבולטים בפרויקט זה יתועדו בקובץ זה.

הפורמט מבוסס על [Keep a Changelog](https://keepachangelog.com/he/1.0.0/),
והפרויקט משתמש ב-[Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### שונה
- **כפתור "שמור שינויים" בסרגל העליון נבנה מחדש** — הכפתור נוצר עד כה ב-JavaScript בלבד, על ידי הזרקת `<li>` לתוך `#wp-admin-bar-root-default` (`assets/js/admin.js`). הוא עבר לרינדור בצד השרת דרך `admin_bar_menu` (`AT_Woo_GF_Integration::add_quick_save_admin_bar_button()`), עם בדיקת `current_user_can( 'edit_post' )`, מחרוזות מתורגמות, escaping ו-`aria-label`. ההיגיון בצד הלקוח צומצם לקשירת ה-click בלבד:
  - השמירה מתבצעת בלחיצה מקורית (`element.click()`) על כפתור ה-submit האמיתי של WordPress — `#publish`, או `#save-post` למשתמש שאינו רשאי לפרסם — כדי שכל מסלול השמירה ירוץ במלואו וששם/ערך ה-submitter ייכללו ב-POST.
  - הנפילה האחרונה ל-`form.submit()` **הוסרה**: היא עוקפת את כל ה-submit handlers (סנכרון TinyMCE, סנכרון שדות WooCommerce, ולידציית ACF) וגם משמיטה את ה-submitter מהמטען. במקומה `form.requestSubmit(submitter)`.
  - `tinyMCE.triggerSave()` נקרא לפני השליחה.
  - ה-`setTimeout` של 5 שניות שהחזיר את הכפתור למצב "שמור שינויים" הוסר — הוא היה מציג כפתור פעיל בזמן ששמירה עדיין רצה ומזמין לחיצה שנייה מיותרת. במקומו `MutationObserver` שמשקף את מצב `#publish` בפועל, כך שהכפתור חוזר לפעולה רק כאשר WordPress עצמו מחזיר את הטופס למצב פעיל (למשל אחרי כשל ולידציה).
- **טופס 20 הפך לטופס תבנית לשכפול במקום לבחירה אוטומטית** — השלמה של הכוונה המקורית של HRV-E5. המימוש הקודם פירש "טופס ברירת מחדל" כ*בחירה אוטומטית* של טופס 20 בשדה "בחר טופס" של כל מוצר אירוע חדש. התוצאה: כל אירוע חדש נקשר בשקט לאותו טופס משותף, וכל ההרשמות של אירועים שונים נערמו על טופס אחד. הבחירה האוטומטית הוסרה מ-`Woo_GF_Product_Form_Metabox::add_product_data_panel()` — מוצר אירוע חדש נפתח כעת עם "-- בחר טופס --" ריק, ומנהל האתר נדרש לבחור טופס קיים או ללחוץ "צור טופס חדש". מוצרים קיימים עם `_woo_gf_form_id` שמור אינם מושפעים: הערך נקרא ישירות מהמטא ומעולם לא נדרס.
- **הפילטר `at_woo_gf_default_form_id` הוסר, ובמקומו `at_woo_gf_template_form_id`** — הפילטר הישן תיאר "איזה טופס לבחור אוטומטית", מושג שכבר אינו קיים. הפילטר החדש עונה על השאלה הנכונה: "איזה טופס משמש תבנית לשכפול". ברירת המחדל היא `Woo_GF_Ajax_Handler::TEMPLATE_FORM_ID` (טופס 20, "טופס לברירת מחדל").

### נוסף
- **"צור טופס חדש" משכפל את טופס התבנית במקום לבנות טופס ריק** — `Woo_GF_Ajax_Handler::create_form()` בנתה עד כה טופס מאפס עם שלושה שדות קשיחים בקוד (שם, אימייל, טלפון), בלי התראות, בלי אישורים ובלי הגדרות הטופס. כעת היא קוראת ל-`GFFormsModel::duplicate_form()` על טופס התבנית — הרוטינה של Gravity Forms עצמה, שמעתיקה שדות, הגדרות, התראות ואישורים — ולאחר מכן משנה שם דרך `GFAPI::update_form()`, מקשרת את הטופס למוצר (`_woo_gf_form_id` דרך CRUD של WooCommerce, תואם HPOS) ומגדירה `woo_gf_linked_product_id` על הטופס החדש. מגבלת הרשמות לפי מלאי המוצר נשמרה.
- **שם הטופס נקבע על ידי מנהל האתר** — לחיצה על "צור טופס חדש" מציגה שדה שם מלא מראש בשם המוצר, שניתן לערוך לפני היצירה. שם ריק נופל חזרה לשם המוצר. GF מבטיח ייחודיות שם, כך שכפילות הופכת ל-"שם (1)" במקום להיכשל.
- הוקמו בפועל שני ה-hooks שתועדו ב-`DEVELOPER.md` אך מעולם לא מומשו בקוד: הפילטר `woo_gf_integration_new_form` (על הטופס המשוכפל לפני השמירה) והאקשן `woo_gf_integration_form_created`.

### אבטחה
- **הידוק ההרשאות ליצירת טופס** — נקודת ה-AJAX `haruv_create_gf_form_for_event` דרשה `edit_products` בלבד, כלומר כל מנהל חנות יכול היה ליצור טפסים ב-Gravity Forms. כעת נדרשות שתי הרשאות במקביל: `gravityforms_create_form` (דרך `GFCommon::current_user_can_any()`, או `manage_options` כגיבוי אם GF לא נטען) **וגם** `edit_post` על המוצר הספציפי. באתר חרוב אין כרגע תפקיד שאינו מנהל מערכת עם הרשאות Gravity Forms, ולכן אין השפעה על משתמשים קיימים.
- שם הטופס עובר `sanitize_text_field` + `wp_unslash`, ומזהה המוצר עובר `absint`.

### תוקן
- **המשך החקירה של "צריך ללחוץ 'עדכן' פעמיים"** — שני התיקונים ב-2.13.4 היו אמיתיים, אך לא היו הסיבה לתסמין שהמשתמש דיווח עליו. הסיבה בפועל **אינה בתוסף הזה** אלא בתמה, ותוקנה שם (`themes/haruv/inc/custom-taxonomies.php`): התמה מסירה את `product_tag` ממוצרים, וסקריפט הטלמטריה של WooCommerce במסך עריכת מוצר (`wc-tracks-product-updated-client-side`) קורא את השדה `tax_input[product_tag]` בלי הגנה וזורק `TypeError` בתוך handler מסוג click על `#publish`. ACF משהה את ה-submit הראשון לצורך ולידציה ומגיש אותו מחדש דרך `.trigger('click')` של jQuery — וה-TypeError קוטע את השרשרת של jQuery לפני הקריאה ל-`element.click()` המקורי. התוצאה: הלחיצה הראשונה על "עדכן" לא שלחה את הטופס כלל. התסמין היה במוצרים בלבד (הסקריפט מותנה בכותרת "עריכת מוצר") ובעריכה בלבד (במוצר חדש הכותרת שונה) — בדיוק כפי שדווח. **כפתור "שמור שינויים" לא היה הגורם**: הכשל שוחזר באופן זהה גם בלחיצה על "עדכן" המקורי של WooCommerce, גם במוצר `simple` שאינו אירוע.
- **כשל שקט ביצירת טופס** — אם טופס התבנית נמחק, הועבר לאשפה או ש-Gravity Forms כבוי, הקוד הישן היה יוצר בשקט טופס ריק שאינו דומה לתבנית. כעת התבנית נבדקת מראש דרך `GFFormsModel::get_form()` (שמחזירה `false` גם לטופס מחוק וגם לטופס באשפה), וכל כשל מחזיר שגיאה מפורשת בעברית שמסבירה מה לעשות — בלי ליצור טופס חלקי. גם `duplicate_form()` ו-`update_form()` נבדקות ל-`WP_Error`.

## [2.13.4] - 2026-07-27

### תוקן
- **צריך ללחוץ "עדכן" פעמיים כדי לשמור אירוע** - שני באגים נפרדים באותו אזור, שניהם תוקנו:
  - **שכתוב חוזר של מטא האירוע ב-`WC_Product_Event::save()`** - הדריסה כתבה את *כל* מפתחות מטא האירוע מתוך ה-props בכל קריאה ל-`$product->save()`, ללא תנאי. המפתח `inquiries_email` ב-`$extra_data` מופה בליבת WooCommerce למטא `_inquiries_email`, בעוד שממשק הניהול שומר ל-`_event_inquiries_email` — ולכן ה-prop תמיד היה ריק והדריסה מחקה את השדה. מכיוון ש-WooCommerce קוראת ל-`$product->save()` שוב מתוך `WC_Meta_Box_Product_Images::save()` (עדיפות 20, כלומר *אחרי* `woocommerce_process_product_meta_event`), לערכים הישנים תמיד הייתה המילה האחרונה. `read_product_data()` שאמור היה למלא את ה-props היה קוד מת מלכתחילה (זו מתודה של ה-data store, לא של `WC_Product`). שתי הדריסות הוסרו — ליבת WooCommerce כבר קוראת וכותבת כל `extra_data` אל `_{key}` לבדה — והמפתח שונה ל-`event_inquiries_email` כך שהוא תואם למטא בפועל (`get_inquiries_email()` / `set_inquiries_email()` נשמרו כ-aliases).
  - **"מספר משתתפים מקסימלי" נטען ריק בעריכה** - הפאנל קרא `$product->get_meta( '_max_attendees' )`. כל מפתח ב-`$extra_data` הופך ל-prop, ו-`WC_Data_Store_WP::filter_raw_meta_data()` מסננת את המטא `_{key}` המקביל מתוך `$product->meta_data`; `WC_Data::get_meta()` מנתבת מפתח כזה ל-getter רק אם הוא כבר רשום ב-`internal_meta_keys` של ה-data store — רשימה שמתמלאת רק בקריאת המטא הראשונה. התוצאה תלוית-סדר: מפתח ה-`_event_*` הראשון שנשאל בבקשה חוזר **ריק**. שדה ריק נשלח כריק, ולכן השמירה הבאה מאפסת את `_max_attendees` ואת `_stock`. כל אתרי הקריאה (פאנל האירוע, מטא-בוקס ה-GF, דשבורד ההרשמות) הועברו ל-getters המוקלדים (`get_max_attendees()`, `get_event_duration()`, `get_event_type()`, `get_event_date()`, `get_event_location()`, `get_event_inquiries_email()`).
- **הגדרות תזמון הטופס לא נשמרו כשהטופס המקושר נמחק** - צד השמירה של `save_product_form_data()` היה עטוף כולו ב-`if ( $form )`, כך ש-`_woo_gf_form_is_active`, `_woo_gf_enable_form_schedule` ו-`_woo_gf_schedule_start/end` לא נשמרו כלל אם הטופס ב-Gravity Forms נמחק/הועבר לאשפה או ש-GFAPI לא זמין. מטא המוצר נכתב כעת ללא תנאי, והסנכרון לאובייקט הטופס בלבד נותר מגודר. משלים את תיקון צד הקריאה מ-HRV-C8.

## [2.13.3] - 2026-07-08

### שונה
- **`uninstall.php` מנקה יותר** - נוספה מחיקת אופציות מודול הסכמת העוגיות (`at_woo_gf_cookie_settings`, `at_woo_gf_cookie_detected`), מטא רשימות ההמתנה (`_event_waitlist_entries`, `enable_waitlist`) וכל ה-transients של `woo_gf_`. השאילתה `SHOW TABLES` עברה ל-`$wpdb->prepare`.
- **i18n**: נוצר `languages/at-woo-gf-integration.pot` (222 מחרוזות) — התוסף מוכן לתרגום.

### תיעוד
- README: עדכון גרסה 2.6.0→2.13.3. DEVELOPER.md: החלפת דומייני העדכונים הישנים (alltech.co.il) במנגנון ה-GitHub raw בפועל + כתובת תמיכה מעודכנת. CHANGELOG: ניקוי סעיף Unreleased מפריטים ששוחררו (CSV, waitlist, עוגיות).

## [2.13.2] - 2026-07-08

### אבטחה
- **בדיקת הרשאות בנקודות AJAX של הדשבורד** - שלוש נקודות (`ajax_get_event_details`, `ajax_get_registrations`, `ajax_clear_dashboard_cache`) אימתו nonce אך לא הרשאה; כעת נדרשת `manage_woocommerce`, כך שמשתמש מחובר חסר-הרשאה אינו יכול לשלוף פרטי נרשמים או לנקות מטמון.
- **הזרקת נוסחאות ב-CSV** - ייצוא הנרשמים השתמש ב-`wp_specialchars` (בריחה ל-HTML, לא ל-CSV); ערך תא שמתחיל ב-`= + - @` היה מורץ כנוסחה באקסל. נוספה `csv_escape_cell()` שמוסיפה prefix `'` ומכפילה מרכאות לפי RFC 4180.
- **`$wpdb->prepare` בניקוי מטמון** - שאילתות מחיקת ה-transients עברו ל-`prepare` + `esc_like`.

### שונה
- הוסרו קריאות `error_log` של מדדי זמן/זיכרון מרינדור הדשבורד; קריאות ה-`error_log` שנותרו (בבלוקי catch) גודרו ב-`WP_DEBUG`. הוסרה שורת `console.log` מה-metabox.

## [2.13.1] - 2026-07-08

### תוקן
- **שדה מחיר נעלם ממוצרים רגילים** - `filter_product_data_tabs()` הוסיף `show_if_event` ללשונית "כללי" עבור *כל* מוצר; יחד עם כלל ה-CSS `.show_if_event { display:none }`, הדבר הסתיר את לשונית הכללי (ואת שדה המחיר) ממוצרים רגילים/פשוטים עד להחלפה לסוג "אירוע". סוג המוצר "event" יורש מ-simple ולכן הלשונית מוצגת ממילא — ההוספה הוסרה.

## [2.13.0] - 2026-07-06

### שונה
- עדכון גרסת release המאחד את שחרור מודול הסכמת העוגיות (2.12.0) ותיקוני החלון והרשימה (2.12.1), וסנכרון עם גרסת התבנית 1.6.0.

## [2.12.1] - 2026-07-06

### תוקן
- **חלון הגדרות עוגיות לא נסגר** - שכבת הרקע (overlay) של החלון נשארה מוצגת (כי `display:flex` גבר על `[hidden]`) ובלעה את כל הלחיצות; כעת `[hidden]` גובר והחלון נסגר כראוי בלחיצה על ✕ / רקע / שמירה.
- **פירוט עוגיות** - נוספה רשימת ברירת מחדל של העוגיות הנפוצות באתר (שם, ספק, מטרה, תפוגה) כך שהפירוט מוצג בחלון ההעדפות עד שהמנהל מגדיר רשימה משלו.

## [2.12.0] - 2026-07-06

### נוסף
- **ניהול הסכמת עוגיות (GDPR)** - מודול עצמאי `class-cookie-consent.php` (נטען גם ללא WooCommerce/Gravity Forms).
  - באנר והעדפות בעברית/RTL: "אשר הכל" / "דחה הכל" / "התאמה אישית" עם בקרה גרנולרית לכל קטגוריה (הכרחיות תמיד פעילות).
  - ההסכמה נשמרת בעוגייה מקומית ומשודרת דרך אירוע `at-cookie-consent` ותכונות data, כך שסקריפטים חיצוניים יכולים לגדר את עצמם עד לקבלת הסכמה.
  - כפתור צף לפתיחה מחדש / משיכת הסכמה.
  - סורק אוטומטי: זיהוי עוגיות שאינן ברשימה ודיווח שלהן לניהול לצורך סיווג.
  - עמוד הגדרות (manage_options): הפעלה/מיקום/טקסט/קישור מדיניות, עריכת שמות ותיאורי קטגוריות, ורשימת עוגיות (שם, קטגוריה, ספק, מטרה, תפוגה).
  - נקודות AJAX (nonce `at_woo_gf_integration_nonce`, `wp_ajax` + `nopriv`) לשמירת בחירה ולדיווח הסורק; אפשרויות עם קידומת `at_woo_gf_cookie_*`.

## [2.11.0] - 2026-06-24

### נוסף
- **רשימת המתנה לאירועים** - מנגנון מלא לרשימת המתנה כאשר אירוע מלא.
  - מחלקה `class-event-waitlist.php` המנהלת אחסון (`_event_waitlist_entries`) ופונקציות API ציבוריות: `woo_gf_is_waitlist_enabled`, `woo_gf_get_waitlist_entries`, `woo_gf_get_waitlist_count`, `woo_gf_add_waitlist_entry`.
  - התוסף הוא המקור היחיד לאחסון רשימת ההמתנה; התמה מאצילה אליו את כל הקריאות.
  - תצוגת רשומות רשימת ההמתנה בדשבורד ההרשמות (טעינה דרך AJAX).

## [2.10.0] - 2026-01-19

### תוקן
- **שמירת תמונה ראשית** - תיקון סופי לבעיה שבה תמונה ראשית של מוצר לא נשמרה במוצרי אירוע.
  - הסרת קריאות מיותרות ל-`$product->save()` בתוך ה-hook של שמירת המטא-דאטה.
  - שימוש ב-`update_post_meta` ישיר כדי למנוע התנגשויות עם מנגנון השמירה המובנה של WooCommerce ו-WordPress.
  - שיפור תאימות עם תהליך השמירה הסטנדרטי של WordPress.

## [2.9.0] - 2026-01-19

### תוקן
- **תזמון סגירת אירוע** - תיקון בעיה שבה שדות התאריך לא נטענו בממשק עריכת המוצר.
  - שיפור מנגנון הטעינה כך שיתועדף המידע מהמטא-דאטה של המוצר (Post Meta).
  - תיקון פורמט התאריכים לשימוש ב-`datetime-local` תקני.
  - סנכרון משופר בין שמירת המוצר לעדכון הטופס ב-Gravity Forms.

## [2.8.0] - 2026-01-19

### תוקן
- **כפתור שמירת שינויים ב-Admin Bar** - תיקון בעיה שבה הכפתור לא עבד עבור מוצרים/פריטים קיימים.
  - הסרת בדיקת הנראות (`:visible`) של jQuery כדי לאפשר לחיצה על כפתורים שעשויים להיות באזורים מוסתרים/מכווצים.
  - הוספת מנגנון fallback שמגיש את הטופס ישירות (`form.submit()`) אם הלחיצה על הכפתור לא מצליחה.
  - שיפור זיהוי כפתורי השמירה והעדכון של WordPress.

## [2.7.2] - 2026-01-19

### תוקן
- **שמירת תמונה ראשית** - תיקון בעיה שבה תמונה ראשית של מוצר לא נשמרה בגלל התנגשות עם מנגנון השמירה של ה-CRUD API של WooCommerce.
  - שינוי לוגיקת השמירה במטא-בוקס לשימוש ב-`update_post_meta` ישיר.
  - מניעת קריאות כפולות ל-`$product->save()` שדרסו שינויים שבוצעו על ידי WordPress core.

## [2.7.1] - 2026-01-19

### תוקן
- **תזמון סגירת אירוע** - תיקון שדות התאריך בממשק עריכת מוצר.
  - הוספת מנגנון שמירה להגדרות תזמון וסטטוס טופס ישירות מהמטא-בוקס של המוצר.
  - תיקון פורמט טעינת תאריכים עבור שדות `datetime-local`.
  - סנכרון דו-כיווני משופר בין המוצר לטופס ה-Gravity Forms המקושר.

## [2.7.0] - 2026-01-19

### תוקן
- **כפתור שמירה מהירה ב-Admin Bar** - תיקון בעיית שמירה של מוצרים קיימים.
  - שיפור מנגנון ה-trigger של השמירה.
  - תמיכה טובה יותר בכפתור "Update" של WordPress.
  - הוספת מנגנון fallback להגשת הטופס במקרה שהקליק נכשל.
  - הארכת זמן ה-timeout של הכפתור לשיפור חיווי ויזואלי.

## [2.6.4] - 2025-12-31

### תוקן
- סטנדרטיזציה של טיפול ושמירת מטא-דאטה של אירועים.

### שונה
- סינון מוצרים לפי סוג אירוע בדשבורד ההרשמה.

## [2.6.3] - 2025-12-28

### שונה
- עדכון גרסה כללי וסנכרון תצורה.

## [2.6.2] - 2025-12-22

### שונה
- עדכון מספרי גרסאות וסנכרון מטא-מידע
- שיפורי תיעוד פנימיים

## [2.6.1] - 2025-12-22

### שונה
- עדכון גרסה תקופתי לסנכרון עם התבנית.
- תיקוני יציבות קלים.

## [2.6.0] - 2025-11-13

### נוסף
- **סטטוס תוכן חדש: "מפורסם אך נגיש בלינק ישיר בלבד"** (`publish-hidden`) - **אוניברסלי לכל סוגי התוכן**
  - עובד על **כל סוגי הפוסטים**: מוצרים, מאמרים, דפים, טפסים וכל post type מותאם אישית
  - תוכן נגיש דרך קישור ישיר (URL) אבל מוסתר מחיפוש ולופים
  - לא מופיע בתוצאות חיפוש באתר (כולל חיפוש רגיל וחיפוש Relevanssi)
  - לא מופיע בדפי ארכיון, קטגוריות, וידג'טים
  - **אינטגרציה מובנית עם Relevanssi** - תוכן מוסתר לא יופיע בתוצאות חיפוש של Relevanssi
    - פילטר `relevanssi_post_ok` - הסתרת פוסטים בודדים
    - פילטר `relevanssi_modify_wp_query` - הסתרה ברמת השאילתה
  - אידיאלי ל:
    - מוצרים פרטיים וקמפיינים ממוקדים
    - מאמרים ודפי נחיתה לקבוצות ספציפיות
    - תוכן ארכיוני שנגיש בקישור ישיר
    - טפסים ותוכן מותאם שדורש שיתוף פרטי
  - ניתן לשינוי בקלות מעמוד עריכת התוכן
  - תווית ברורה ומתארת: "מפורסם אך נגיש בלינק ישיר בלבד"
  - מדריך שימוש מלא: `docs/HIDDEN-STATUS-GUIDE.md`

## [2.5.5] - 2025-11-13

### תוקן
- **תפריט ניהול** - תיקונים בסדר וארגון התפריט
  - הוספת ריווח (separator) לפני ואחרי "דשבורד הרשמות"
  - הוספת תפריט WooCommerce הראשי בחזרה (היה מוסתר)
  - תיקון תצוגה של "פיתוח ידע ומחקר" (מאמרים) - הפריט מופיע כעת בתפריט
  - שיפור זיהוי אוטומטי של פריט Posts בתפריט

## [2.5.4] - 2025-11-13

### נוסף
- **הצהרת תאימות HPOS** - התוסף מצהיר כעת על תאימות מלאה ל-WooCommerce Custom Order Tables (HPOS)
  - שימוש ב-`FeaturesUtil::declare_compatibility()`
  - תומך ב-High-Performance Order Storage
  - מסיר את ההודעה "incompatible plugins" ב-WooCommerce

### תוקן
- **Warning "Array to string conversion"** - תיקון ב-pagination של טבלת ההרשמות
- **כפתור "צור טופס חדש" במוצר חדש** - שיפור UX כשהמוצר טרם נשמר
  - הצגת dialog ידידותי שמסביר שצריך לשמור את המוצר תחילה
  - אפשרות לשמור אוטומטית כטיוטה ולהמשיך
  - בדיקת כותרת מוצר לפני שמירה
  - הודעות משופרות עם אייקונים ברורים (✅ ❌ 📝 💡)
- **Sidepeek נרשמים לא נסגר** - תיקון מלא של האינטראקציה
  - כפתור X עובד כעת (event delegation תוקן)
  - לחיצה על overlay סוגרת את ה-sidepeek
  - מקש ESC סוגר את ה-sidepeek
  - הודעת "אין נרשמים" מוצגת במקום spinner נצחי
  - אנימציה חלקה (slide מימין + fade overlay)
- **כפתור ייצוא כשאין נרשמים** - UX משופר משמעותית
  - הכפתור disabled אוטומטית כשאין הרשמות (opacity 0.5, אפור)
  - Tooltip ברור: "אין הרשמות לייצוא"
  - לחיצה על disabled מציגה הודעה ידידותית במקום שגיאה
  - עיצוב disabled ברור וויזואלי

## [2.5.3] - 2025-11-13

### תוקן
- **בעיית Timeout בדשבורד הרשמות** - העמוד `admin.php?page=event-registrations` היה מחזיר 524 Gateway Timeout
  - הוספת caching מלא לכל הפונקציות הכבדות (transient cache)
  - הסרת debug queries כבדים שרצו בכל טעינת דף
  - אופטימיזציה של שאילתות DB עם `fields => 'ids'` במקום לשלוף נתונים מלאים
  - הגבלת מספר מוצרים ל-100 למניעת בעיות זיכרון
  - batch fetching של טפסים Gravity Forms (שליפה אחת במקום הרבה)
- **שיפור ביצועים משמעותי**:
  - הוספת cache ל-`get_events_data()` (2 דקות)
  - הוספת cache ל-`get_total_entries_count()` (5 דקות)
  - הוספת cache ל-`get_today_entries_count()` (5 דקות)
  - הוספת cache ל-`get_yesterday_entries_count()` (24 שעות)
  - הוספת cache ל-`get_registration_count()` (2 דקות)
  - הוספת cache ל-`get_events_count()` (5 דקות)
  - הוספת cache ל-`get_active_events_count()` (5 דקות)

### נוסף
- **מנגנון ניקוי cache** - כפתור "רענן" מנקה את כל המטמונים
- **טיפול בשגיאות משופר** - try-catch blocks במקומות קריטיים
- **הגדרת timeout** - set_time_limit(60) למניעת תקיעות
- **debug info משופר** - מידע דיבאג קל משקל יותר שלא גורם ל-timeout

### שופר
- העמוד כעת נטען תוך 1-3 שניות במקום timeout
- טעינה ראשונה יוצרת cache, טעינות הבאות מיידיות
- הגבלה חכמה של מספר הטפסים לספירה (<50 טפסים)

## [2.5.2] - 2025-10-19

### תוקן
- **בעיית הצגת הרשמות** - הרשמות כעת מוצגות גם אם לא שולמו או אם המטא `woo_gf_product_id` לא נשמרה
  - הסרת פילטר `status => active` שהגביל הצגת רק הרשמות משולמות
  - הוספת fallback להצגת כל ההרשמות מהטופס אם לא נמצאו הרשמות עם קישור למוצר
  - תיקון בכל המקומות: דשבורד הרשמים, מסך עריכת מוצר, ספירת משתתפים, ייצוא
  - כעת כל ההרשמות מוצגות ללא קשר לסטטוס תשלום
- **בעיית שמירת שדות אירוע** - הוספת hook fallback `woocommerce_process_product_meta` כדי לוודא ששדות נשמרים גם אם ה-hook הספציפי לא עובד
- שדות תאריך, שעה, מספר משתתפים וכל שאר הנתונים כעת נשמרים בהצלחה ב-100% מהמקרים

## [2.5.1] - 2025-10-19

### שופר
- **טבלת מוצרים מותאמת אישית** - הצגת עמודות חיוניות בלבד לממשק נקי וממוקד
  - מציג רק: תמונה, שם, מלאי, מחיר, תאריך, סוג האירוע, שפות
  - מסיר עמודות מיותרות: קטגוריות, תגיות, SKU, מומלץ, קלאסיפיקציה למשלוח, קהלי יעד
  - ממשק נקי וקל לניהול עם המידע החשוב ביותר

### תוקן
- **בעיה 404 בטעינת דשבורד CSS/JS** - תיקון שם ה-hook מ-`woocommerce_page_at-woo-gf-registrations` ל-`toplevel_page_event-registrations`
- **Debug logging** - הוספת הודעות debug לזיהוי בעיות טעינת assets

## [2.5.0] - 2025-10-19

### נוסף
- **ארגון תפריט ניהול מותאם אישית** - סידור מחדש של תפריט הניהול:
  - דשבורד הרשמות בנפרד בראש התפריט
  - מוצרים, טפסים (רק למנהלים) והזמנות בקבוצה ראשונה
  - מדיה, פיתוח ידע ומחקר, CPTs בקבוצה שנייה
  - עמודים נגישים רק למנהלים
- **קופונים כתפריט משני** - הוספת קופונים כתפריט משני תחת "טפסים"
- **Separators לארגון ויזואלי** - הפרדה ברורה בין קבוצות תפריטים

### שופר
- **שינוי לייבל Posts** ל"פיתוח ידע ומחקר" בתפריט הראשי
- **טעינת CSS מותאמת** - קובץ dashboard-custom.css נטען בכל דפי הניהול לעיצוב התפריט

## [2.4.0] - 2025-10-19

### תוקן
- **בעיית שמירה של שדות אירוע** - כל השדות (תאריך, שעה, מספר משתתפים וכו') כעת נשמרים בהצלחה
- **טיפול בשדות ריקים** - השדות לא יישמרו עם ערכים שגויים כשהוא ריק
- **סינכרון עם Gravity Forms** - מספר משתתפים מקסימלי מסתנכרן אוטומטית עם מגבלת הרשמות בטופס

### נוסף
- **סינכרון אוטומטי** - כשמעדכנים מספר משתתפים מקסימלי, ההגבלה בטופס Gravity Forms מתעדכנת גם כן
- **ניהול משקל** - המערכת מאפשרת ניהול משקל ב-WooCommerce כדי לעקוב אחרי משתתפים

### שופר
- **הערה על שדה מספר משתתפים** - הוספת הערה שהשדה יסתנכרן עם Gravity Forms
- **בדיקות אבטחה** - הוספת Nonce verification לשמירה

## [2.3.0] - 2025-10-19

### נוסף
- **כפתור ייצוא לאקסל** בטבלת האירועים
  - ייצוא של כל הנרשמים לטופס כקובץ CSV
  - שם קובץ אוטומטי עם שם האירוע וחותמת זמן
  - שימוש ב-GFAPI של Gravity Forms לקריאת הנתונים
  - טעינה אסינכרונית ללא טעינת דף מחדש
  - הודעות שגיאה ידידותיות

### שופר
- **כפתורים בעמודת פעולות** - צמצום גדלי הכפתורים לצמודים טקסט
- **ממשק משתמש יותר יעיל** - אייקונים בלבד עם title tooltips

## [2.2.0] - 2025-10-19

### נוסף
- **יכולות מיון לטבלת האירועים**:
  - מיון לפי תאריך התחלה (עולה/יורד)
  - מיון לפי מחיר (זול ביותר/יקר ביותר)
  - מיון לפי סטטוס טופס (סגור/פתוח)
- **לחצים מיון בכותרות** - עם אינדיקטורים חזותיים (↑/↓)
- **שמירת מצב מיון** - URL query parameters

### שופר
- **שם אירוע** הוא קישור ישיר לעריכת המוצר
- **שם הטופס** הוא קישור ישיר לעריכת הטופס ב-Gravity Forms
- **הוספת תאריך סגירה אוטומטית** - מוצג אם הגדיר בטופס Gravity Forms
- **כמות כרטיסים** - הצגת קיבולת עם ∞ אם ללא הגבלה
- **הסרת תגית אירוע** - מיותרת בטבלה

## [2.1.1] - 2025-10-19

### שופר
- **הסרת עמודת תמונה** מטבלת האירועים בדשבורד - חיסכון במקום וקריאות טובה יותר
- **צמצום עמודות נרשמים ומקומות פנויים** לעמודה משולבת אחת בשם "משתתפים"
  - הצגת מספר נרשמים ויחס קיבולת (למש"כ: 5/30)
  - סרגל קיבולת ויזואלי
  - מספר מקומות פנויים
- **שדה סטטוס משודרג** להציג "✅ פתוח" או "❌ סגור" בהתאם להיות הטופס Gravity Forms פעיל

## [2.1.0] - 2025-10-19

### נוסף
- **שדה "פרוייקטור אחראי"** למוצרים מסוג אירוע לזיהוי אחראי האירוע
- **עמודה מחיר** בטבלת דשבורד האירועים - הצגת מחיר או "חינמי"
- **תצוגה משודרגת של תאריך ושעה** בטבלת האירועים, מוצגים יחד בעמודה אחת
- **עמודה פרוייקטור אחראי** בטבלת האירועים לקל-זיהוי אחראי האירוע

### שופר
- תצוגת מידע כללי בדשבורד הרשמות עם פרטים מלאים יותר
- טבלת האירועים כעת מציגה את כל המידע החשוב בקצרה
- **הסרת עמודת תמונה** מטבלת האירועים לחיסכון במקום
- **צמצום נרשמים ומקומות פנויים** לעמודה משולבת אחת
- **שדה סטטוס** משודרג להציג "✅ פתוח" או "❌ סגור" בהתאם להיות הטופס פעיל

## [2.0.0] - 2025-10-19

### נוסף
- **מערכת הרשאות משתמשים מתקדמת** עם 3 רמות גישה:
  - 👨‍💻 **Administrator (Developer)** - גישה מלאה לכל המערכת
  - ✍️ **Content Manager (מנהל תוכן)** - ניהול תוכן ואירועים ללא גישה למערכת
  - 📅 **Event Editor (עורך אירועים)** - עריכת אירועים בלבד
- **תפריט ניהול אירועים מותאם** לכל סוג משתמש
- **אינדיקטור תפקיד** בסרגל הניהול העליון
- **הודעות ברוכים הבאים** מותאמות לכל תפקיד
- **הגבלות גישה** לעמודים רגישים (תוספים, תבניות, הגדרות)
- **דף ייצוא נתונים** למנהלי תוכן
- **דשבורד ניהול אירועים** עם סטטיסטיקות מהירות
- **גישה מהירה** לפעולות נפוצות בסרגל העליון
- **מעקב פעילות אחרונה** בדשבורד

### שופר
- **אבטחה מוגברת** עם הגבלות גישה לפי תפקידים
- **ממשק משתמש מותאם** לכל סוג משתמש
- **ביצועים משופרים** עם טעינה דינמית של תפריטים
- **תמיכה מלאה ב-RTL** בכל הממשקים החדשים
- עדכון דרישות מערכת ל-WordPress 6.0+ ו-PHP 7.4+
- עדכון תאימות ל-WooCommerce 8.9

### אבטחה
- **הגבלת עריכת קבצים** למפתחים בלבד
- **חסימת התקנת תוספים/תבניות** למשתמשים לא מורשים
- **בדיקת הרשאות** בכל דף ניהול
- **הגנה על עמודי הגדרות** מפני גישה לא מורשית

### מעבר מגרסאות קודמות
- כל המשתמשים הקיימים ישמרו על ההרשאות שלהם
- Administrators יקבלו אוטומטית הרשאות Developer
- Editors יקבלו הרשאות לניהול אירועים
- ניתן ליצור משתמשים חדשים עם התפקידים החדשים

## [1.0.1] - 2024-01-18

### תוקן
- תוקנה בעיה שבה שדות מחיר ושדות בסיסיים לא הוצגו במוצרי אירוע
- הוספת אישור להצגת שדות מחיר, מחיר מבצע, SKU וניהול מלאי במוצרי אירוע

### שופר
- הוספת אייקונים מתאימים לכל שדות האירוע (תאריך, מיקום, משתתפים וכו')
- הוספת אייקונים לכפתורים ולשדות בטאב Gravity Forms
- שיפור עיצוב פאנל האירועים עם צבעים ייחודיים לאייקונים
- הוספת אמוג'י לאפשרויות סוג האירוע (פיזי/מקוון/משולב)
- שיפור עיצוב סרגל ההתקדמות במספר המשתתפים

## [1.0.0] - 2024-01-18

### נוסף
- שיוך טפסי Gravity Forms למוצרי WooCommerce
- יצירת טפסים אוטומטית עם שדות בסיסיים
- תצוגת הרשמות ישירות בעמוד עריכת המוצר
- דשבורד הרשמות מרכזי עם סטטיסטיקות וחיפוש מתקדם
- סוג מוצר "אירוע" עם ניהול משתתפים
- מערכת עדכונים מרחוק
- תאימות מלאה עם WooCommerce 8.5
- תמיכה ב-RTL

### תוקן
- שימוש בשיטות מודרניות של WooCommerce לניהול metadata
- תאימות עם מבנה הנתונים המעודכן של WooCommerce

### אבטחה
- הוספת בדיקות nonce לכל הפעולות AJAX
- סניטציה של כל הקלטים

## [2.13.4] - 2026-07-27
### תכנון עתידי
- אינטגרציה עם LearnDash
- שליחת אימיילים אוטומטיים למשתתפים
- מודול התראות back-office (טפסים ללא נרשמים מעל 5 ימים)

_בוצע: ייצוא CSV (2.13.0), ניהול רשימות המתנה (2.11), מודול הסכמת עוגיות (2.12)._